# PDF2CAD 图纸解析平台 —— GPU 可选组件
# 与 requirements.txt 叠加安装（先装基础依赖，再装本文件）：
#   pip install -r requirements.txt
#   pip install -r requirements-gpu.txt
#
# 仅建议在带 NVIDIA GPU（CUDA）的机器上安装；本仓库运行环境（无 GPU、3GB 内存）
# 禁止安装以下重依赖。所有组件均为 lazy import 探测，缺失时平台自动优雅降级。

# ---- YOLO 图纸元素检测后端（core/backends.py convert_yolo）----
# 检测图框/标题栏/表格/符号，检测框写入 DXF 的 DETECTION 图层。
# 需自备图纸元素检测权重（UI 高级选项中配置路径）。
ultralytics>=8.2,<9.0        # YOLOv8/v11 推理框架（自带 torch 依赖）

# ---- PaddleOCR 引擎（core/ocr_engines.py，PP-OCRv5）----
# 中文/旋转文字检出率优于 tesseract；可用后在 UI「OCR 引擎」下拉中出现。
paddlepaddle-gpu>=2.6,<3.3   # Paddle 深度学习框架 GPU 版（需匹配本机 CUDA 版本）
paddleocr>=2.8               # PP-OCRv5 文字检测+识别（含方向分类器）

# ---- VLM 标题栏结构化抽取（core/vlm.py，Qwen2.5-VL）----
# 从标题栏区域抽取 图名/图号/比例/日期/设计/审核/单位，结构化写入 DXF 与
# title_block.json。默认模型 Qwen/Qwen2.5-VL-3B-Instruct（约 6GB 显存）。
torch>=2.2                   # PyTorch（GPU 版请按 CUDA 版本从 pytorch.org 源安装）
transformers>=4.49           # Qwen2.5-VL 模型加载（Qwen2_5_VLForConditionalGeneration）
qwen-vl-utils>=0.0.10        # Qwen-VL 视觉输入预处理工具
accelerate>=0.30             # device_map="auto" 多卡/显存调度
