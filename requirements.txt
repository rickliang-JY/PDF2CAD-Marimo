# PDF2CAD 图纸解析平台 —— 必需依赖
# 运行环境：无 GPU、3GB 内存。禁止将 torch/ultralytics 列为必需项。
marimo>=0.24
pymupdf>=1.24        # import pymupdf（fitz 旧接口已 deprecated）
ezdxf>=1.3
opencv-python-headless>=4.8
numpy>=1.24
matplotlib>=3.7
scikit-image>=0.21

# ---- 可选依赖（缺失时平台自动优雅降级，不影响主流程）----
# pytesseract        # OCR 文本识别与 CV+ 文字掩膜（另需系统二进制 tesseract）
# vtracer            # CV+ 可选曲线通道（use_vtracer=True 时启用）
# ultralytics        # YOLO 图纸元素检测（体积大，3GB 内存环境不建议安装）
